import React,{useEffect,useMemo,useRef,useState} from 'react';import{createRoot}from'react-dom/client';import{useReactTable,getCoreRowModel,getSortedRowModel,getFilteredRowModel,getPaginationRowModel,flexRender,type ColumnDef}from'@tanstack/react-table';import{sampleStatement}from'./lib/sampleData';import{fmtMoney,fmtInt,D,fmtRate}from'./lib/format';import{groupArtists,groupReleases,groupTracks,groupBy,totals,rateSummary,periodSortValue,searchRows,deductionFields}from'./lib/analytics';import{downloadCsv,toCsv,normalisedRows}from'./lib/exportCsv';import type{StatementData,Transaction}from'./lib/types';import type{ImportProgress}from'./lib/importProgress';import{isLargeFile}from'./lib/importProgress';import'./styles.css';
const tabs=['Overview','Artists','Releases','Tracks','Shops','Countries','Sales Periods','Deductions','Full Detail','Import Checks'];
function App(){const[data,setData]=useState<StatementData>();const[err,setErr]=useState<ImportErrorState|null>(null);const[progress,setProgress]=useState<ImportProgress|null>(null);const[fileMeta,setFileMeta]=useState<{name:string;size:number}|null>(null);const[elapsed,setElapsed]=useState(0);const[stalled,setStalled]=useState(false);const worker=useRef<Worker|null>(null);const lastProgress=useRef(0);useEffect(()=>{if(!progress)return;const id=setInterval(()=>{setElapsed(Date.now()-lastProgress.current);setStalled(Date.now()-lastProgress.current>20000)},500);return()=>clearInterval(id)},[progress]);async function load(f:File){cancel(false);setErr(null);setFileMeta({name:f.name,size:f.size});lastProgress.current=Date.now();setElapsed(0);setStalled(false);setProgress({stage:'Reading file',percent:0,rowsExamined:0,rowsImported:0,rowsSkipped:0,largeFileMode:isLargeFile(f.size)});try{const t=performance.now();const reader=new FileReader();reader.onprogress=e=>{if(e.lengthComputable)setProgress(p=>monotonic(p,{stage:'Reading file',percent:Math.round((e.loaded/e.total)*15),rowsExamined:0,rowsImported:0,rowsSkipped:0,largeFileMode:isLargeFile(f.size)}))};reader.onerror=()=>setErr({stage:'Reading file',message:'This statement could not be fully processed.',details:String(reader.error),rowsProcessed:0});reader.onload=()=>{const buffer=reader.result as ArrayBuffer;const w=new Worker(new URL('./importWorker.ts',import.meta.url),{type:'module'});worker.current=w;w.onmessage=e=>{if(e.data.type==='progress'){lastProgress.current=Date.now();setStalled(false);setProgress(p=>monotonic(p,{...e.data.progress,largeFileMode:e.data.progress.largeFileMode??isLargeFile(f.size)}));}else if(e.data.type==='complete'){lastProgress.current=Date.now();setProgress(p=>monotonic(p,{stage:'Complete',percent:100,rowsExamined:e.data.data.rows.length,rowsImported:e.data.data.rows.length,rowsSkipped:e.data.data.diagnostics.blankRowsIgnored,largeFileMode:e.data.data.diagnostics.largeFileMode,message:'Statement ready'}));setTimeout(()=>{setData(e.data.data);setProgress(null);setFileMeta(null);w.terminate();worker.current=null},500)}else if(e.data.type==='error'){setErr({stage:progress?.stage??'Opening workbook',message:friendly(e.data.error.message),details:e.data.error.details,rowsProcessed:progress?.rowsExamined??0});setProgress(null);w.terminate();worker.current=null;}};w.onerror=e=>{setErr({stage:progress?.stage??'Opening workbook',message:'This statement could not be fully processed.',details:e.message,rowsProcessed:progress?.rowsExamined??0});setProgress(null);w.terminate();worker.current=null};w.postMessage({buffer,filename:f.name,fileSize:f.size,fileReadMs:performance.now()-t},[buffer]);};reader.readAsArrayBuffer(f)}catch(e){setErr({stage:progress?.stage??'Reading file',message:'This statement could not be fully processed.',details:e instanceof Error?e.stack||e.message:String(e),rowsProcessed:progress?.rowsExamined??0});setProgress(null)}}function cancel(clear=true){worker.current?.terminate();worker.current=null;if(clear){setProgress(null);setFileMeta(null);setErr(null);setStalled(false)}}return <>{!data?<Upload onFile={load} onSample={()=>setData(sampleStatement())} err={err} progress={progress} file={fileMeta} stalled={stalled} elapsed={elapsed} cancel={()=>cancel(true)}/>:<Dashboard data={data} remove={()=>setData(undefined)}/>}</>}
type ImportErrorState={stage:string;message:string;details?:string;rowsProcessed:number};
function monotonic(prev:ImportProgress|null,next:ImportProgress){return {...next,percent:Math.max(prev?.percent??0,next.percent)}}
function friendly(message:string){if(/memory|allocation/i.test(message))return'The browser ran out of memory while opening this statement. No data was uploaded or stored.';if(/recognised|missing/i.test(message))return'The statement structure could not be recognised.';return message||'This statement could not be fully processed.'}
function Upload(p:{onFile:(f:File)=>void;onSample:()=>void;err:ImportErrorState|null;progress:ImportProgress|null;file:{name:string;size:number}|null;stalled:boolean;elapsed:number;cancel:()=>void}){if(p.progress&&p.file)return <main className="upload"><section className="processing"><h1>Processing your statement…</h1><p>Large statements may take a little longer. Please keep this page open.</p>{p.progress.largeFileMode&&<aside className="mode"><b>Large File Mode</b><br/>This statement will be processed in smaller sections to reduce browser memory use.<br/>For very large statements, CSV may import faster than Excel.</aside>}<p aria-live="polite"><b>{p.progress.message??p.progress.stage}</b></p><div className="bar" role="progressbar" aria-valuemin={0} aria-valuemax={100} aria-valuenow={p.progress.percent} aria-label="Import progress"><span style={{width:`${p.progress.percent}%`}}/></div><dl><dt>Filename</dt><dd>{p.file.name}</dd><dt>File size</dt><dd>{formatBytes(p.file.size)}</dd><dt>Current stage</dt><dd>{p.progress.stage}</dd><dt>Progress percentage</dt><dd>{p.progress.percent}%</dd><dt>Rows examined</dt><dd>{p.progress.rowsExamined}</dd><dt>Rows imported</dt><dd>{p.progress.rowsImported}</dd><dt>Rows skipped</dt><dd>{p.progress.rowsSkipped}</dd><dt>Current worksheet</dt><dd>{p.progress.currentWorksheet??'—'}</dd><dt>Elapsed time</dt><dd>{Math.round(p.elapsed/1000)}s</dd></dl>{p.stalled&&<p className="note">This is taking longer than expected, but processing is still running.</p>}<button onClick={p.cancel}>Cancel import</button></section></main>;return <main className="upload"><section><h1>Cargo Statement Analyser</h1><p>Please upload your Details digital statements for summaries and analysis.</p><div className="drop" onDragOver={e=>e.preventDefault()} onDrop={e=>{e.preventDefault();const f=e.dataTransfer.files[0];if(f)p.onFile(f)}}><strong>Drop your Cargo statement here</strong><span>Supported formats: .xlsx, .xls, .csv</span><label className="button">Choose file<input type="file" accept=".xlsx,.xls,.csv" onChange={e=>{const f=e.target.files?.[0];if(f)p.onFile(f)}}/></label></div><aside className="privacy">Your statement is analysed entirely in your browser. Nothing is uploaded or stored.</aside>{p.err&&<section className="error"><h2>This statement could not be fully processed.</h2><p>{p.err.message}</p><p>Stage where processing failed: {p.err.stage} · Rows processed before failure: {p.err.rowsProcessed}</p><details><summary>Technical details</summary><pre>{p.err.details}</pre></details><button onClick={()=>location.reload()}>Try another file</button></section>}<button onClick={p.onSample}>Load sample statement</button></section></main>}
function formatBytes(size:number){return size>1024*1024?`${(size/1024/1024).toFixed(1)} MB`:`${(size/1024).toFixed(1)} KB`}
function Dashboard({data,remove}:{data:StatementData;remove:()=>void}){const[tab,setTab]=useState('Overview');const[filters,setFilters]=useState<Record<string,string>>({});const[quick,setQuick]=useState<{field:keyof Transaction;q:string;exact?:boolean}|null>(null);const filtered=useMemo(()=>data.rows.filter(r=>Object.entries(filters).every(([k,v])=>!v||String((r as any)[k]).toLowerCase().includes(v.toLowerCase()))),[data,filters]);const isUnfiltered=!Object.values(filters).some(Boolean);const initial=data.diagnostics.initialSummary;const t=useMemo(()=>totals(filtered),[filtered]);const releases=useMemo(()=>tab==='Releases'||!isUnfiltered?groupReleases(filtered):[],[filtered,tab,isUnfiltered]);const tracks=useMemo(()=>tab==='Tracks'||!isUnfiltered?groupTracks(filtered):[],[filtered,tab,isUnfiltered]);const artists=useMemo(()=>tab==='Artists'||!isUnfiltered?groupArtists(filtered):[],[filtered,tab,isUnfiltered]);const rates=useMemo(()=>isUnfiltered&&initial?{label:initial.royaltyRateSummary,rates:[]}:rateSummary(filtered),[filtered,isUnfiltered,initial]);const focused=quick?.q?searchRows(filtered,quick.field,quick.q,quick.exact,data.indexes):[];return <main className="app"><header><div><h1>Cargo Statement Analyser</h1><p>Please upload your Details digital statements for summaries and analysis.</p><p><b>{data.label}</b> · {data.filename} {data.diagnostics.account&&`· Account: ${data.diagnostics.account}`} {data.diagnostics.reportingPeriod&&`· Reporting period: ${data.diagnostics.reportingPeriod}`}</p></div><button onClick={remove}>Remove statement</button></header><Filters filters={filters} setFilters={setFilters} rows={data.rows}/><Quick setQuick={setQuick}/>{quick&&<Focused rows={focused} clear={()=>setQuick(null)}/>}<section className="cards"><Card title="Your earnings" value={isUnfiltered&&initial?fmtMoney(initial.totalRoyaltyAmount):fmtMoney(t.royaltyAmount)}/><Card title="Revenue before your royalty rate" value={isUnfiltered&&initial?fmtMoney(initial.totalAmount):fmtMoney(t.amount)}/><Card title="Sales / usages" value={isUnfiltered&&initial?fmtInt(initial.totalSales):fmtInt(t.sales)}/><Card title="Returns" value={isUnfiltered&&initial?fmtInt(initial.totalReturns):fmtInt(t.returns)}/><Card title="Deductions and line charges" value={isUnfiltered&&initial?fmtMoney(initial.totalDeductions):fmtMoney(t.deductions)}/><Card title="Transaction rows" value={String(filtered.length)}/><Card title="Releases" value={String(isUnfiltered&&initial?initial.uniqueReleaseCount:releases.length)}/><Card title="Tracks" value={String(isUnfiltered&&initial?initial.uniqueTrackCount:tracks.length)}/><Card title="Artists" value={String(isUnfiltered&&initial?initial.uniqueArtistCount:artists.length)}/><Card title="Royalty rate" value={rates.label}/></section><nav>{tabs.map(x=><button className={tab===x?'active':''} onClick={()=>setTab(x)} key={x}>{x}</button>)}</nav>{tab==='Overview'&&<Overview rows={filtered}/>} {tab==='Artists'&&<Summary rows={artists} cols={['artist','royaltyAmount','amount','sales','returns','releaseCount','trackCount','shopCount','countryCount','transactionRows']} name="artist summary"/>}{tab==='Releases'&&<Summary rows={releases} cols={['albumTitle','artist','releaseCode','catalogNumber','barcode','royaltyAmount','amount','sales','returns','trackCount','shopCount','countryCount','transactionRows']} name="release summary"/>}{tab==='Tracks'&&<Summary rows={tracks} cols={['artist','trackTitle','isrc','albumTitle','catalogNumber','barcode','royaltyAmount','amount','sales','returns','shopCount','countryCount','transactionRows']} name="track summary"/>}{tab==='Shops'&&<Summary rows={groupBy(filtered,'shop')} cols={['shop','royaltyAmount','amount','sales','returns','artistCount','releaseCount','trackCount','transactionRows']} name="shop summary"/>}{tab==='Countries'&&<Summary rows={groupBy(filtered,'country')} cols={['country','royaltyAmount','amount','sales','returns','artistCount','releaseCount','trackCount','transactionRows']} name="country summary"/>}{tab==='Sales Periods'&&<><p className="note">Sales Period is when the underlying usage or sale occurred. It may be earlier than the statement reporting month.</p><Summary rows={groupBy(filtered,'salesPeriod').sort((a,b)=>periodSortValue(a.salesPeriod).localeCompare(periodSortValue(b.salesPeriod)))} cols={['salesPeriod','royaltyAmount','amount','sales','returns','shopCount','countryCount','transactionRows']} name="sales-period summary"/></>}{tab==='Deductions'&&<Deductions rows={filtered}/>} {tab==='Full Detail'&&<Detail rows={filtered}/>} {tab==='Import Checks'&&<Checks data={data}/>}</main>}
function Card(p:{title:string;value:string}){return <div className="card"><span>{p.title}</span><b>{p.value}</b></div>}
function Filters({filters,setFilters,rows}:{filters:Record<string,string>;setFilters:(f:Record<string,string>)=>void;rows:Transaction[]}){const fields=[['artist','Artist'],['albumTitle','Release'],['trackTitle','Track'],['catalogNumber','Catalogue number'],['barcode','Barcode'],['isrc','ISRC'],['shop','Shop'],['country','Country'],['usageType','Usage Type'],['assetType','Asset Type'],['salesPeriod','Sales Period'],['royaltyRate','Royalty Rate']];return <details className="filters"><summary>Global filters · {Object.values(filters).filter(Boolean).length} active</summary><div>{fields.map(([k,l])=><label key={k}>{l}<input value={filters[k]??''} onChange={e=>setFilters({...filters,[k]:e.target.value})}/></label>)}<button onClick={()=>setFilters({})}>Clear filters</button></div></details>}
function Quick({setQuick}:{setQuick:(q:any)=>void}){const qs=[['artist','Search artist'],['albumTitle','Search release'],['trackTitle','Search track'],['catalogNumber','Search catalogue number'],['barcode','Search barcode'],['isrc','Search ISRC']];return <section className="quick">{qs.map(([f,l])=><label key={f}>{l}<input onKeyDown={e=>{if(e.key==='Enter')setQuick({field:f,q:(e.target as HTMLInputElement).value,exact:['catalogNumber','barcode','isrc'].includes(f)})}} placeholder="Press Enter"/></label>)}</section>}
function Focused({rows,clear}:{rows:Transaction[];clear:()=>void}){const t=totals(rows);return <section className="focused"><button onClick={clear}>Clear selected result</button><h2>Focused result</h2><div className="cards"><Card title="Your earnings" value={fmtMoney(t.royaltyAmount)}/><Card title="Revenue before royalty rate" value={fmtMoney(t.amount)}/><Card title="Sales / usages" value={fmtInt(t.sales)}/><Card title="Returns" value={fmtInt(t.returns)}/><Card title="Transaction count" value={String(rows.length)}/><Card title="Releases included" value={String(groupReleases(rows).length)}/><Card title="Tracks included" value={String(groupTracks(rows).length)}/></div><Overview rows={rows}/></section>}
function Overview({rows}:{rows:Transaction[]}){return <section className="grid"><Summary rows={groupBy(rows,'usageType').slice(0,10)} cols={['usageType','royaltyAmount','amount','sales','returns','transactionRows']} name="usage type summary"/><Summary rows={groupBy(rows,'shop').slice(0,10)} cols={['shop','royaltyAmount','amount','sales','returns']} name="shop earnings"/><Summary rows={groupBy(rows,'country').slice(0,10)} cols={['country','royaltyAmount','amount','sales','returns']} name="country earnings"/><Summary rows={groupArtists(rows).slice(0,10)} cols={['artist','royaltyAmount','amount','sales','returns']} name="top artists"/></section>}
function Summary({rows,cols,name}:{rows:any[];cols:string[];name:string}){return <section className="panel"><button onClick={()=>downloadCsv(`${name}.csv`,toCsv(rows.map(r=>Object.fromEntries(cols.map(c=>[c,String(r[c] instanceof Object&&r[c].toString?r[c].toString():r[c]??'')]))),cols))}>Export {name}</button><table><thead><tr>{cols.map(c=><th key={c}>{c}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={i}>{cols.map(c=><td key={c}>{['amount','royaltyAmount','deductions','lineCharges'].includes(c)?fmtMoney(r[c]):['sales','returns'].includes(c)?fmtInt(r[c]):String(r[c]??'')}</td>)}</tr>)}</tbody></table>{!rows.length&&<p className="empty">No rows to show.</p>}</section>}
function Deductions({rows}:{rows:Transaction[]}){const totalsBy=deductionFields.map(f=>({field:f,total:rows.reduce((a,r)=>a.plus(D(r[f])),D(0))}));const any=totalsBy.some(x=>!x.total.isZero());return <section>{!any&&<p>No deductions or line charges were found in this statement.</p>}<Summary rows={totalsBy} cols={['field','total']} name="deductions summary"/><Summary rows={groupArtists(rows.filter(r=>deductionFields.some(f=>!D(r[f]).isZero())))} cols={['artist','deductions','transactionRows']} name="deductions by artist"/></section>}
function Detail({rows}:{rows:Transaction[]}){const [globalFilter,setGlobalFilter]=useState('');const cols=useMemo<ColumnDef<Transaction>[]>(()=>Object.keys(rows[0]??{sourceSheet:'',sourceRow:0}).filter(k=>k!=='originalRow').map(k=>({accessorKey:k,header:k,cell:i=>String(i.getValue()??'')})),[rows]);const table=useReactTable({data:rows,columns:cols,state:{globalFilter,pagination:{pageIndex:0,pageSize:50}},onGlobalFilterChange:setGlobalFilter,getCoreRowModel:getCoreRowModel(),getSortedRowModel:getSortedRowModel(),getFilteredRowModel:getFilteredRowModel(),getPaginationRowModel:getPaginationRowModel()});const visible=table.getFilteredRowModel().rows.map(r=>r.original);const t=totals(visible);return <section className="panel"><input placeholder="Global search" value={globalFilter} onChange={e=>setGlobalFilter(e.target.value)}/><button onClick={()=>setGlobalFilter('')}>Clear filters</button><button onClick={()=>downloadCsv('filtered-results.csv',toCsv(normalisedRows(visible)))}>Export filtered results</button><button onClick={()=>downloadCsv('full-detail.csv',toCsv(normalisedRows(rows)))}>Export full detail</button><p>Transaction count: {visible.length} · Filtered Your earnings: {fmtMoney(t.royaltyAmount)} · Amount: {fmtMoney(t.amount)} · Sales: {fmtInt(t.sales)} · Returns: {fmtInt(t.returns)}</p><table className="detail"><thead>{table.getHeaderGroups().map(hg=><tr key={hg.id}>{hg.headers.map(h=><th key={h.id} onClick={h.column.getToggleSortingHandler()}>{flexRender(h.column.columnDef.header,h.getContext())}</th>)}</tr>)}</thead><tbody>{table.getRowModel().rows.map(r=><tr key={r.id}>{r.getVisibleCells().map(c=><td key={c.id}>{flexRender(c.column.columnDef.cell,c.getContext())}</td>)}</tr>)}</tbody></table><button onClick={()=>table.previousPage()}>Previous</button><button onClick={()=>table.nextPage()}>Next</button></section>}
function Checks({data}:{data:StatementData}){const d=data.diagnostics,t=totals(data.rows);const[dups,setDups]=useState<number|null>(d.largeFileMode?null:d.duplicateLookingRows);return <section className="panel"><button onClick={()=>setDups(findDuplicates(data.rows))}>Run duplicate check</button><p className="note">This check may take longer on large statements.</p><dl><dt>duplicateLookingRows</dt><dd>{dups??'Deferred'}</dd>{Object.entries(d).map(([k,v])=><React.Fragment key={k}><dt>{k}</dt><dd>{typeof v==='object'?JSON.stringify(v):String(v)}</dd></React.Fragment>)}<dt>Total Amount</dt><dd>{t.amount.toString()}</dd><dt>Total Royalty Amount</dt><dd>{t.royaltyAmount.toString()}</dd><dt>Total Sales</dt><dd>{t.sales.toString()}</dd><dt>Total Returns</dt><dd>{t.returns.toString()}</dd><dt>Total deductions</dt><dd>{t.deductions.toString()}</dd><dt>Total line charges</dt><dd>{t.lineCharges.toString()}</dd></dl></section>}
function findDuplicates(rows:Transaction[]){const seen=new Map<string,number>();for(const r of rows){const key=[r.artist,r.albumTitle,r.trackTitle,r.isrc,r.shop,r.country,r.salesPeriod,r.usageType,r.sales,r.amount,r.royaltyAmount].join('|');seen.set(key,(seen.get(key)??0)+1)}return[...seen.values()].filter(v=>v>1).reduce((a,v)=>a+v,0)}
createRoot(document.getElementById('root')!).render(<App/>);
