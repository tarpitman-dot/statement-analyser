import Decimal from 'decimal.js';
import * as XLSX from 'xlsx';
import { columnAliases, fieldLabels, requiredFields, resolveHeader, type FieldKey } from './columnAliases';
import { D, text } from './format';
import { recordTimingValue, safeNow } from './timing';
import { clampProgress, isLargeFile, type ImportProgress } from './importProgress';
import { releaseKey, isTrackRow } from './analytics';
import type { ImportDiagnostics, ImportSummary, StatementData, Transaction } from './types';
const textFields = new Set<FieldKey>(['contractId','releaseCode','catalogNumber','barcode','isrc','country']);
const CHUNK_SIZE=3000;
export interface ParseOptions{onProgress?:(p:ImportProgress)=>void;largeFileMode?:boolean;fileReadMs?:number;estimatedRows?:number;readOptions?:XLSX.ParsingOptions;sheetJsReadAttempt?:string;retryAttemptNumber?:number;}
export function parseFilename(filename:string){const m=filename.match(/^([^_]+)___(\d{4})_(\d{2})_(\d{2})___(\d{4})_(\d{2})_(\d{2})/); if(!m)return{}; const f=(y:string,mo:string,d:string)=>new Date(Date.UTC(+y,+mo-1,+d)).toLocaleDateString('en-GB',{day:'numeric',month:'long',year:'numeric',timeZone:'UTC'}); return {account:m[1],reportingPeriod:`${f(m[2],m[3],m[4])} to ${f(m[5],m[6],m[7])}`};}
export function normaliseIdentifier(v:unknown){const s=text(v); if(/^\d+(\.\d+)?e\+?\d+$/i.test(s)){return Number(s).toLocaleString('fullwide',{useGrouping:false});} return s;}
export function parseRoyaltyRate(value:unknown){if(value===null||value===undefined)return null; const raw=typeof value==='string'?value.trim():value; if(raw==='')return null; const stringValue=String(raw).trim(); const isPercentage=stringValue.includes('%'); const cleaned=stringValue.replace(/,/g,'').replace(/%/g,'').trim(); if(!cleaned)return null; try{const rate=new Decimal(cleaned); if(!rate.isFinite())return null; return isPercentage||rate.abs().gt(1)?rate.div(100):rate;}catch{return null;}}
function isBlankRow(row:unknown[]){return row.every(c=>text(c)==='');}
export function detectHeaderRow(rows:unknown[][]){let best={idx:-1,count:0,map:new Map<FieldKey,number>()}; for(let i=0;i<Math.min(30,rows.length);i++){const map=new Map<FieldKey,number>(); (rows[i]??[]).forEach((h,j)=>{const k=resolveHeader(text(h)); if(k&&!map.has(k))map.set(k,j);}); const req=requiredFields.filter(f=>map.has(f)).length; const score=map.size+req*3; if(score>best.count) best={idx:i,count:score,map};} return requiredFields.every(f=>best.map.has(f))?best:null;}
function tx(row:unknown[], map:Map<FieldKey,number>, sheet:string, sourceRow:number, diag:ImportDiagnostics, keepOriginal:boolean):Transaction{const rowWarnings:string[]=[];const get=(k:FieldKey)=>{const v=row[map.get(k)??-1]; if(k==='royaltyRate'){const parsed=parseRoyaltyRate(v); if(parsed===null&&text(v)!==''){const msg=`${sheet} row ${sourceRow}: Royalty Rate could not be parsed (${text(v)})`;diag.invalidNumericValues.push(msg);rowWarnings.push(msg)} return parsed?.toString()??'';} return textFields.has(k)?normaliseIdentifier(v):text(v);}; const out:Transaction={sourceSheet:sheet,sourceRow,contractId:get('contractId'),contractName:get('contractName'),shareContract:get('shareContract'),assetType:get('assetType'),releaseCode:get('releaseCode'),albumTitle:get('albumTitle'),catalogNumber:get('catalogNumber'),barcode:get('barcode'),isrc:get('isrc'),artist:get('artist'),trackTitle:get('trackTitle'),usageType:get('usageType'),country:get('country'),shop:get('shop'),salesPeriod:get('salesPeriod'),sales:get('sales'),returns:get('returns'),ppu:get('ppu'),amount:get('amount'),share:get('share'),rata1:get('rata1'),rata2:get('rata2'),deduction1:get('deduction1'),deduction2:get('deduction2'),deduction3:get('deduction3'),contractDeductions:get('contractDeductions'),deduction4:get('deduction4'),deduction5:get('deduction5'),lineCharges:get('lineCharges'),royaltyRate:get('royaltyRate'),royaltyAmount:get('royaltyAmount')}; if(keepOriginal)out.originalRow=Object.fromEntries(row.map((v,i)=>[String(i),v])); if(rowWarnings.length)out.rowWarnings=rowWarnings; return out;}
function rangeRows(ws:XLSX.WorkSheet,start:number,end:number){const ref=ws['!ref'];if(!ref)return[];const range=XLSX.utils.decode_range(ref);range.s.r=start;range.e.r=Math.max(start,end-1);return XLSX.utils.sheet_to_json(ws,{header:1,defval:'',raw:false,range,blankrows:false}) as unknown[][];}
function sheetLastRow(ws:XLSX.WorkSheet){const ref=ws['!ref']; if(!ref)return 0; return XLSX.utils.decode_range(ref).e.r+1;}
function summary(rows:Transaction[]):ImportSummary{let totalAmount=D(0),totalRoyaltyAmount=D(0),totalSales=D(0),totalReturns=D(0),totalDeductions=D(0),totalLineCharges=D(0);const artists=new Set<string>(),releases=new Set<string>(),tracks=new Set<string>(),rates=new Set<string>();for(const r of rows){totalAmount=totalAmount.plus(D(r.amount));totalRoyaltyAmount=totalRoyaltyAmount.plus(D(r.royaltyAmount));totalSales=totalSales.plus(D(r.sales));totalReturns=totalReturns.plus(D(r.returns));totalDeductions=totalDeductions.plus(['deduction1','deduction2','deduction3','contractDeductions','deduction4','deduction5','lineCharges'].reduce((s,k)=>s.plus(D((r as any)[k])),D(0)));totalLineCharges=totalLineCharges.plus(D(r.lineCharges));if(r.artist)artists.add(r.artist);releases.add(releaseKey(r));if(isTrackRow(r))tracks.add(r.isrc||`${r.artist}|${r.albumTitle}|${r.trackTitle}`);if(r.royaltyRate)rates.add(r.royaltyRate)}return{totalAmount:totalAmount.toString(),totalRoyaltyAmount:totalRoyaltyAmount.toString(),totalSales:totalSales.toString(),totalReturns:totalReturns.toString(),totalDeductions:totalDeductions.toString(),totalLineCharges:totalLineCharges.toString(),transactionCount:rows.length,uniqueArtistCount:artists.size,uniqueReleaseCount:releases.size,uniqueTrackCount:tracks.size,royaltyRateSummary:rates.size===0?'Rate not supplied':rates.size===1?`${D([...rates][0]).mul(100).toString()}%`:'Multiple rates'}}
export function parseWorkbook(wb:XLSX.WorkBook, filename='statement', fileSize=0, opts:ParseOptions={}):StatementData{let pct=25;const started=safeNow();const emit=(stage:any,f=0,extra={})=>{pct=clampProgress(pct,stage,f);opts.onProgress?.({stage,percent:pct,rowsExamined:diag.transactionRows+diag.blankRowsIgnored,rowsImported:rows.length,rowsSkipped:diag.blankRowsIgnored,...extra});}; const rows:Transaction[]=[]; const diag:ImportDiagnostics={filename,fileSize,...parseFilename(filename),worksheetsDetected:wb.SheetNames,worksheetsImported:[],worksheetsIgnored:[],headerRows:{},transactionRows:0,blankRowsIgnored:0,detectedColumns:[],missingOptionalColumns:[],missingRequiredColumns:[],invalidNumericValues:[],unclassifiedDateValues:[],blankArtistValues:0,blankReleaseValues:0,blankIsrcValues:0,bundleRowsWithBlankIsrc:0,duplicateLookingRows:0,royaltyValidation:{matches:0,requiresReview:0,notChecked:0},largeFileMode:opts.largeFileMode,estimatedRows:opts.estimatedRows,importTimings:{fileReadingMs:opts.fileReadMs,totalImportMs:0,tabFirstCalculationMs:{},searchIndexMs:{}}}; const names=[...wb.SheetNames].sort((a,b)=>(a==='Digital Sales'?-1:b==='Digital Sales'?1:0)); const tHead=safeNow(); const imports:{name:string;h:ReturnType<typeof detectHeaderRow>;last:number}[]=[]; names.forEach((name,i)=>{const ws=wb.Sheets[name];const last=sheetLastRow(ws);const preview=rangeRows(ws,0,Math.min(30,last));const h=preview.length&&!preview.every(isBlankRow)?detectHeaderRow(preview):null;if(!h)diag.worksheetsIgnored.push(`${name} (${last?'no recognised statement table':'blank'})`);else imports.push({name,h,last});emit('Detecting worksheets and headers',(i+1)/names.length,{currentWorksheet:name});}); recordTimingValue(diag,'headerDetectionMs',safeNow()-tHead); if(!imports.length){const anyHeader=names.some(name=>rangeRows(wb.Sheets[name],0,Math.min(30,sheetLastRow(wb.Sheets[name]))).some(r=>r.some(c=>resolveHeader(text(c))))); if(anyHeader) throw new Error(`The statement is missing the following required columns: ${requiredFields.map(f=>fieldLabels[f]).join(', ')}`); throw new Error('The statement structure could not be recognised.');} const estimated=imports.reduce((a,s)=>a+Math.max(0,s.last-(s.h?.idx??0)-1),0); diag.estimatedRows=estimated; diag.largeFileMode=isLargeFile(fileSize,estimated)||!!opts.largeFileMode; const keepOriginal=!diag.largeFileMode; const tRows=safeNow(); let processed=0; for(const it of imports){diag.worksheetsImported.push(it.name); diag.headerRows[it.name]=(it.h?.idx??0)+1; diag.detectedColumns=[...new Set([...diag.detectedColumns,...[...it.h!.map.keys()].map(k=>fieldLabels[k])])]; for(let start=it.h!.idx+1;start<it.last;start+=CHUNK_SIZE){const chunk=rangeRows(wb.Sheets[it.name],start,Math.min(it.last,start+CHUNK_SIZE)); for(let j=0;j<chunk.length;j++){const sourceRow=start+j+1;try{const row=chunk[j];processed++; if(isBlankRow(row)){diag.blankRowsIgnored++;continue;} rows.push(tx(row,it.h!.map,it.name,sourceRow,diag,keepOriginal));}catch(e){diag.invalidNumericValues.push(`${it.name} row ${sourceRow}: ${e instanceof Error?e.message:String(e)}`);}} diag.transactionRows=rows.length; emit('Importing transactions',estimated?processed/estimated:1,{currentWorksheet:it.name});}}
 recordTimingValue(diag,'rowNormalisationMs',safeNow()-tRows); diag.missingRequiredColumns=requiredFields.filter(f=>!diag.detectedColumns.includes(fieldLabels[f])).map(f=>fieldLabels[f]); diag.missingOptionalColumns=(Object.keys(columnAliases) as FieldKey[]).filter(f=>!requiredFields.includes(f)&&!diag.detectedColumns.includes(fieldLabels[f])).map(f=>fieldLabels[f]); if(diag.missingRequiredColumns.length) throw new Error(`The statement is missing the following required columns: ${diag.missingRequiredColumns.join(', ')}`); const tSum=safeNow(); enrichDiagnostics(rows,diag,{skipDuplicates:diag.largeFileMode}); diag.initialSummary=summary(rows); recordTimingValue(diag,'initialSummaryMs',safeNow()-tSum); emit('Calculating initial summaries',1); recordTimingValue(diag,'dashboardPreparationMs',1); emit('Preparing dashboard',1); recordTimingValue(diag,'totalImportMs',safeNow()-started+(opts.fileReadMs??0)); emit('Complete',1); return {label:'Uploaded statement',filename,fileSize,rows,diagnostics:diag};}
export const DEFAULT_SHEETJS_READ_OPTIONS:XLSX.ParsingOptions={type:'array',cellDates:false,cellStyles:false,cellHTML:false,bookVBA:false,bookDeps:false,bookFiles:false,sheetStubs:false};
export const CONSERVATIVE_SHEETJS_READ_OPTIONS:XLSX.ParsingOptions={type:'array',cellDates:false,cellStyles:false,cellHTML:false,bookVBA:false,bookDeps:false,bookFiles:false,sheetStubs:true,dense:false};
export async function parseArrayBuffer(buf:ArrayBuffer,filename:string,fileSize:number,opts:ParseOptions={}){const t=safeNow(); if(!buf||buf.byteLength===0)throw new Error('Workbook ArrayBuffer is empty before SheetJS read.'); opts.onProgress?.({stage:'Opening workbook',percent:15,rowsExamined:0,rowsImported:0,rowsSkipped:0,message:opts.sheetJsReadAttempt??'Opening workbook'}); const wb=XLSX.read(buf,{...DEFAULT_SHEETJS_READ_OPTIONS,...opts.readOptions,type:'array'}); const opening=safeNow()-t; const data=parseWorkbook(wb,filename,fileSize,opts); recordTimingValue(data.diagnostics,'workbookOpeningMs',opening); return data;}
export async function parseFile(file:File,opts:ParseOptions={}){const t=safeNow(); const buf=await file.arrayBuffer(); opts.onProgress?.({stage:'Reading file',percent:15,rowsExamined:0,rowsImported:0,rowsSkipped:0}); return parseArrayBuffer(buf,file.name,file.size,{...opts,fileReadMs:safeNow()-t});}
export function enrichDiagnostics(rows:Transaction[],d:ImportDiagnostics,opts:{skipDuplicates?:boolean}={}){const seen=new Map<string,number>(); for(const r of rows){try{if(!r.artist)d.blankArtistValues++; if(!r.albumTitle&&!r.catalogNumber&&!r.barcode&&!r.releaseCode)d.blankReleaseValues++; if(!r.isrc)d.blankIsrcValues++; if(!r.isrc&&/bundle/i.test(`${r.assetType} ${r.usageType}`))d.bundleRowsWithBlankIsrc++; if(!opts.skipDuplicates){const key=[r.artist,r.albumTitle,r.trackTitle,r.isrc,r.shop,r.country,r.salesPeriod,r.usageType,r.sales,r.amount,r.royaltyAmount].join('|'); seen.set(key,(seen.get(key)??0)+1);} const adjusted=['share','rata1','rata2','deduction1','deduction2','deduction3','contractDeductions','deduction4','deduction5','lineCharges'].some(k=>!D((r as any)[k]).isZero()); if(adjusted||!r.royaltyRate){d.royaltyValidation.notChecked++;} else if(D(r.amount).mul(D(r.royaltyRate)).minus(D(r.royaltyAmount)).abs().lte(0.0001)) d.royaltyValidation.matches++; else d.royaltyValidation.requiresReview++;}catch(e){d.invalidNumericValues.push(`${r.sourceSheet} row ${r.sourceRow}: ${e instanceof Error?e.message:String(e)}`); d.royaltyValidation.notChecked++;}} d.duplicateLookingRows=opts.skipDuplicates?0:[...seen.values()].filter(v=>v>1).reduce((a,v)=>a+v,0);}
